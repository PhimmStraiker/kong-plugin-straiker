# tokenusage service
Tracks API token usage across teams. Two services: tokenusage-api, tokenusage-web.
Build: `make build && make test`. Deploy: `./deploy.sh staging`.
Config in config.yaml. Entry point app.py.
